<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainScreen">

    <!-- Set OnClickListener to trigger results when pressed -->
    <Button
        android:id="@+id/add_note_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="69dp"
        android:layout_marginLeft="41dp"
        android:text="Create new note"
        android:layout_below="@+id/note_listview"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
<!--

    &lt;!&ndash; Set OnClickListener to trigger results when pressed &ndash;&gt;
    <Button
        android:id="@+id/add_board_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="20dp"
        android:text="Create new board" />
    -->

    <ListView
        android:id="@+id/note_listview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="20dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/note_creation_area"
        android:hint="Start typing a note!"
        android:layout_alignTop="@+id/note_listview"
        android:layout_alignLeft="@+id/add_note_button"
        android:layout_alignStart="@+id/add_note_button" />

</RelativeLayout>
